/*Problem Statement:
Build a graph with n vertices and m edges using adjacency matrix representation.
The graph may be directed or undirected.

Input Format:
- First line contains n (vertices)
- Second line contains m (edges)
- Third line contains type (0 for undirected, 1 for directed)
- Next m lines contain pairs (u, v)

Output Format:
- Print n x n adjacency matrix

Example:
Input:
4
3
0
0 1
1 2
2 3

Output:
0 1 0 0
1 0 1 0
0 1 0 1
0 0 1 0*/

#include <stdio.h>

int main() {
    int n, m, type;
    scanf("%d", &n);
    scanf("%d", &m);
    scanf("%d", &type);

    int adj[n][n];

    for (int i = 0; i < n; i++)
        for (int j = 0; j < n; j++)
            adj[i][j] = 0;

    for (int i = 0; i < m; i++) {
        int u, v;
        scanf("%d %d", &u, &v);

        adj[u][v] = 1;

        if (type == 0)
            adj[v][u] = 1;
    }

    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++)
            printf("%d ", adj[i][j]);
        printf("\n");
    }

    return 0;
}
